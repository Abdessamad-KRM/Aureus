<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Config - Phase 2 Sécurité
    ✅ HTTPS Only pour tous les domaines
    ⚠️ Certificate Pinning désactivé pour éviter blocage quand Google change ses certificats
-->
<network-security-config>
    <!-- Configuration de base - seulement HTTPS -->
    <base-config>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <!-- ✅ PHASE 2: Domaines Firebase - HTTPS obligatoire -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebasestorage.googleapis.com</domain>
        <domain includeSubdomains="true">firestore.googleapis.com</domain>
        <domain includeSubdomains="true">firebase-auth.googleapis.com</domain>
        <domain includeSubdomains="true">firebaseio.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
    </domain-config>

    <!-- Aucun autre domaine autorisé en HTTP -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">.</domain>
    </domain-config>

    <!-- Debug uniquement - permettre tous les certificats pour développement -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>